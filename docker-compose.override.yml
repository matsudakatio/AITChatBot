# ローカル開発用設定（docker compose up で自動適用される）
# 研究室Macで本番モードで動かしたいときは
#   docker compose -f docker-compose.yml up -d --build
# のように override を読み込まないコマンドを使う

services:
  web:
    volumes:
      - .:/app
      - vectorstore_data:/app/vectorstore
    environment:
      - FLASK_DEBUG=1
